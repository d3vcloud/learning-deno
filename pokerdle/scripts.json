{
  "scripts": {
    "build": "deno bundle index.ts > dist/index.js",
    "start": "deno run --import-map=import_map.json --allow-net=pokeapi.co --allow-env=GOD_MODE index.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "dev": "deno run --watch --import-map=import_map.json --allow-net=pokeapi.co --allow-write=./solution.txt --allow-env=GOD_MODE index.ts"
  }
}
